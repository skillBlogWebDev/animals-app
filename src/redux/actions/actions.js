export const userSuccess = (type, payload) => {
  return {
    type,
    payload,
  };
};

export const userError = (type) => {
  return {
    type,
  };
};

export const appointmentsSuccess = (type, payload) => {
  return {
    type,
    payload,
  };
};

export const appointmentsError = (type) => {
  return {
    type,
  };
};

export const animalsSuccess = (type, payload) => {
  return {
    type,
    payload,
  };
};

export const animalsError = (type) => {
  return {
    type,
  };
};
